// Layout cho route group (dashboard) — admin, pending-access, dashboard pages
export default function DashboardGroupLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
